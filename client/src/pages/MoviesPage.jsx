import SectionPage from "./SectionPage";

const MoviesPage = () => (
  <SectionPage
    type="movie"
    title="Movies"
    subtitle="Track films and series from first watchlist spark to final credits."
  />
);

export default MoviesPage;
