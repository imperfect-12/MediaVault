import SectionPage from "./SectionPage";

const GamesPage = () => (
  <SectionPage
    type="game"
    title="Games"
    subtitle="Follow your backlog, current runs, completed adventures, and forever favourites."
  />
);

export default GamesPage;
