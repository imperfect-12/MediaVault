import SectionPage from "./SectionPage";

const AnimePage = () => (
  <SectionPage
    type="anime"
    title="Anime"
    subtitle="Keep seasons, arcs, favourites, and dropped shows in one calm shelf."
  />
);

export default AnimePage;
